/**
 * Where the API server lives.
 *
 * `localhost` works from the iOS Simulator because it shares your Mac's network.
 * On a real phone, `localhost` means the phone itself — so to test on a device,
 * change this to your Mac's address on the same Wi-Fi, e.g.
 * 'http://192.168.1.20:3000'. (Find it with: ipconfig getifaddr en0)
 */
export const API_BASE_URL = 'http://localhost:3000';
